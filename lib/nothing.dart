import 'package:flutter/material.dart';
class nithing extends StatelessWidget {
  const nithing({key});
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Column(
        children: [
          Text('ssdddds'),
        ],
      ),
    );
  }



}
